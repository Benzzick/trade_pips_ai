import 'package:flutter/material.dart';

class AppColors {
  static const primary = Colors.white;
  static const background = Color.fromRGBO(3, 1, 29, 1);
  static const secondary = Color.fromRGBO(3, 26, 52, 1);
  static const tertiary = Color.fromRGBO(34, 138, 255, 1);
}
